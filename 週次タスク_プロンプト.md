# 週次おすすめ生成タスク（Claude Code on web スケジュール用プロンプト）

> 使い方: 下の「=== プロンプト本文 ===」以下をそのままコピーし、Claude Code on web の
> スケジュールタスク（週1回）のプロンプトに貼り付ける。リポジトリは dashboard を紐付け済みとする。

=== プロンプト本文 ===

あなたは、リポジトリ直下の傾向分析DBに基づいて「今週のおすすめ作品」を更新する自動タスクです。以下を厳密に実行してください。

## 前提・禁止事項
- 参照する嗜好モデルは `profile/profile.json`、除外リストは `profile/seen.csv`、既推薦は `recommend/history.json`。
- **編集してよいのは `recommend/` 配下のみ**。`profile/` やサイト本体（index.html 等）は絶対に変更しない。
- **dアニメストアには一切アクセスしない**。情報収集は WebSearch のみ。
- 事実（配信/巻数/受賞/放送時期）は必ず出典URLを添え、【確定】（出典で確認）/【未確認】（噂・観測）を区別する。作品評価・軸当てはめは「Claudeの解釈」と明示する。

## 手順
1. 今日の日付を取得（`date +%F`）。`profile/profile.json`・`profile/seen.csv`・`recommend/history.json` を読む。
2. `profile.json` の `axes`（7軸）、`scoring.formula`（`A + 1.5*B + C + D + E + G - 2*F`）、`rule`（判別ルール）、`clusters`（特に「深層核」）を理解する。
3. WebSearch で候補を収集（各媒体を横断：アニメ新作/今季/来季、マンガ、小説、VN/ゲーム、話題作・受賞作）。検索例：「2026年 夏アニメ 一覧」「今 話題 SF アニメ」「◯◯ 受賞 マンガ」等。深層核（親密な関係性B×喪失D×思弁E×知的密度G、俯瞰的最適化Fが低い）に合致しそうなものを重点的に。
4. 各候補を7軸（0/1/2）で採点（あらすじ・構成を検索で確認したうえでの解釈）。`scoring.formula` で適合スコアを算出。
5. **除外**：`seen.csv` のフランチャイズに該当（題名の正規化一致・部分一致）、または `history.json` に既出のタイトルは除外。
6. 適合スコア順に上位 **6〜8作** を選定（可能なら媒体を分散：アニメ/マンガ/小説/VN）。深層核クラスタ適合を優先し、次点・気分でも数点含めてよい。
7. 出力を書き込む：
   - `recommend/recommend.json` を後述スキーマで**上書き**。
   - `recommend/history.json` の `recommended_titles` に今回のタイトルを追記（重複させない）、`updated` を今日の日付に。
   - `recommend/news/<今日の日付>.md` に、今週のトップ3程度の短いダイジェスト（出典URL・確定/未確認の区別つき）を記録。
8. **コミットは `recommend/` のみ**：
   ```
   git add recommend/
   git commit -m "chore: weekly recommend update <今日の日付>"
   git push -u origin <デフォルトブランチ>
   ```
   push が失敗したらネットワーク起因のみ指数バックオフ（2s,4s,8s,16s）で最大4回再試行。
9. 変更が無い週（新規候補ゼロ）は、その旨を news に1行記録し、recommend.json は据え置き（不要なコミットをしない）。

## recommend.json スキーマ（上書き）
```json
{
  "generated": "YYYY-MM-DD",
  "basis": "profile/profile.json <version>",
  "note": "tier=本命/次点/気分で, fit=適合スコア(A+1.5B+C+D+E+G-2F), cluster=深層核/思弁/情念関係/雰囲気",
  "items": [
    {
      "tier": "本命",
      "title": "作品名",
      "media": "アニメ|マンガ|小説|VN/ゲーム",
      "cluster": "深層核",
      "fit": 9.0,
      "axes": {"A":1,"B":2,"C":2,"D":2,"E":2,"F":0,"G":2},
      "why": "なぜ深層核に符合するか（Claudeの解釈）",
      "facts": "配信/巻数/受賞など（【確定】/【未確認】区別）",
      "sources": ["https://..."]
    }
  ]
}
```

## 品質チェック（コミット前）
- recommend.json / history.json が有効なJSONか（パースできるか）。
- 除外漏れが無いか（seen/history と突き合わせ）。
- 各 item に sources が1つ以上、facts に確定/未確認の区別があるか。
- `recommend/` 以外に変更が無いか（`git status` で確認。あれば取り消す）。

=== プロンプト本文ここまで ===
